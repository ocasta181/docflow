[project]
name = "pliage"
version = "0.1.1"
description = "Small command-line utilities for document workflows"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
keywords = [
    "documents",
    "jpeg",
    "ocr",
    "pdf",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Office/Business",
    "Topic :: Utilities",
]
dependencies = []

[project.scripts]
batch-ocr = "pliage.legacy.batch_ocr:main"
jpg2pdf = "pliage.legacy.jpg2pdf:main"
pdftools = "pliage.legacy.pdftools:main"
pliage = "pliage.cli.app:main"

[project.optional-dependencies]
pdf = [
    "pypdf>=4.0",
]
image = [
    "Pillow>=10.0",
    "reportlab>=4.0",
]
ocr = [
    "pymupdf>=1.24",
]
all = [
    "Pillow>=10.0",
    "pymupdf>=1.24",
    "pypdf>=4.0",
    "reportlab>=4.0",
]
dev = [
    "pyright>=1.1",
    "pyinstaller>=6.0",
    "pytest>=8.0",
    "ruff>=0.8",
]

[project.urls]
Homepage = "https://github.com/ocasta181/pliage"
Issues = "https://github.com/ocasta181/pliage/issues"
Repository = "https://github.com/ocasta181/pliage"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pliage"]

[tool.ruff]
line-length = 100

[tool.pyright]
include = ["src"]
pythonVersion = "3.10"
typeCheckingMode = "basic"
reportMissingTypeStubs = false
